spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo
    username: root
    password: pass
  mail:
    host: smtp.naver.com
    port: 465
    username: syhoneyjam@naver.com
    password: ${NAVER_MAIL_APP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true

api:
  bearer-token: ${GITHUB_API_BEARER_TOKEN}